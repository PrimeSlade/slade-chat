services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: slade-backend-dev
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - '${PORT:-3001}:3001'
    volumes:
      - .:/app
      - /app/node_modules
